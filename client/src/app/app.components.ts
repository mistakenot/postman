import {AppComponent} from './app';
import {
  Home, 
  About, 
  Auth, AuthLogin, AuthLoginForm, AuthLoginExternal, AuthLogout,
  NavBar, NavLinks, NavPagination,
  Inbox, InboxList, InboxListItem, InboxActiveItem} from './components';

export {AppComponent} from './app';

export const Components = [
  AppComponent,
  Home,
  About,
  Auth, AuthLogin, AuthLoginForm, AuthLoginExternal, AuthLogout,
  NavBar, NavLinks, NavPagination,
  Inbox, InboxList, InboxListItem, InboxActiveItem
]
